const userData=require('./users');
const groupData=require('./groups');
const idolData=require('./idols');
const blogData=require('./blogs');
const storyData=require('./story');

module.exports=
{
    users: userData,
    groups: groupData,
    idols: idolData,
    blogs: blogData,
    story: storyData,
}